@model IEnumerable<SignalR.Models.StateType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StateName)
            </th>
          
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StateName)
            </td>
          
            <td>
                <a href="/StateType/Edit/@item.Id">Edit</a> |
                <a href="/StateType/Delete/@item.Id">Delete</a>
            </td>
        </tr>
        }
    </tbody>
</table>
